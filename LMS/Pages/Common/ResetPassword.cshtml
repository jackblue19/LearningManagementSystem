@page
@model LMS.Pages.Common.ResetPasswordModel
@{
    ViewData["Title"] = "Đặt lại mật khẩu";
}

<style>
    body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .reset-container {
        background: white;
        padding: 40px;
        border-radius: 20px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        max-width: 450px;
        width: 100%;
        margin: 20px;
    }

    .reset-header {
        text-align: center;
        margin-bottom: 30px;
    }

    .reset-header h2 {
        color: #667eea;
        margin-bottom: 10px;
        font-size: 28px;
        font-weight: bold;
    }

    .reset-header p {
        color: #666;
        font-size: 14px;
    }

    .form-group {
        margin-bottom: 20px;
    }

    .form-group label {
        display: block;
        margin-bottom: 8px;
        color: #333;
        font-weight: 500;
        font-size: 14px;
    }

    .form-control {
        width: 100%;
        padding: 12px 15px;
        border: 2px solid #e0e0e0;
        border-radius: 10px;
        font-size: 14px;
        transition: all 0.3s;
        box-sizing: border-box;
    }

    .form-control:focus {
        outline: none;
        border-color: #667eea;
        box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }

    .btn-reset {
        width: 100%;
        padding: 15px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
        border-radius: 10px;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: transform 0.2s;
        margin-top: 10px;
    }

    .btn-reset:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
    }

    .alert {
        padding: 12px 15px;
        border-radius: 10px;
        margin-bottom: 20px;
        font-size: 14px;
    }

    .alert-danger {
        background-color: #fee;
        color: #c33;
        border: 1px solid #fcc;
    }

    .alert-success {
        background-color: #efe;
        color: #3c3;
        border: 1px solid #cfc;
    }

    .text-danger {
        color: #c33;
        font-size: 12px;
        margin-top: 5px;
        display: block;
    }

    .back-to-login {
        text-align: center;
        margin-top: 20px;
    }

    .back-to-login a {
        color: #667eea;
        text-decoration: none;
        font-size: 14px;
        transition: color 0.3s;
    }

    .back-to-login a:hover {
        color: #764ba2;
        text-decoration: underline;
    }
</style>

<div class="reset-container">
    <div class="reset-header">
        <h2>🔒 Đặt lại mật khẩu</h2>
        <p>Nhập mật khẩu mới cho tài khoản của bạn</p>
    </div>

    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger">
            @Model.ErrorMessage
        </div>
    }

    @if (Model.IsSuccess)
    {
        <div class="alert alert-success">
            <strong>Thành công!</strong> Mật khẩu đã được đặt lại. Đang chuyển hướng đến trang đăng nhập...
        </div>
        <script>
            setTimeout(function() {
                window.location.href = '/Common/Login';
            }, 2000);
        </script>
    }
    else
    {
        <form method="post">
            <input type="hidden" asp-for="Model.Token" />
            <input type="hidden" asp-for="Model.Email" />

            <div class="form-group">
                <label asp-for="Model.Password">Mật khẩu mới</label>
                <div class="input-group">
                    <input type="password" class="form-control" asp-for="Model.Password" placeholder="Nhập mật khẩu mới (tối thiểu 6 ký tự)" id="resetPassword" />
                    <button class="btn btn-outline-secondary" type="button" onclick="togglePassword('resetPassword', 'resetIcon')">
                        <i class="bi bi-eye" id="resetIcon"></i>
                    </button>
                </div>
                <span asp-validation-for="Model.Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Model.ConfirmPassword">Xác nhận mật khẩu</label>
                <div class="input-group">
                    <input type="password" class="form-control" asp-for="Model.ConfirmPassword" placeholder="Nhập lại mật khẩu mới" id="resetConfirmPassword" />
                    <button class="btn btn-outline-secondary" type="button" onclick="togglePassword('resetConfirmPassword', 'resetConfirmIcon')">
                        <i class="bi bi-eye" id="resetConfirmIcon"></i>
                    </button>
                </div>
                <span asp-validation-for="Model.ConfirmPassword" class="text-danger"></span>
            </div>

            <button type="submit" class="btn-reset">Đặt lại mật khẩu</button>
        </form>

        <div class="back-to-login">
            <a href="/Common/Login">← Quay lại đăng nhập</a>
        </div>
    }
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function togglePassword(inputId, iconId) {
            const input = document.getElementById(inputId);
            const icon = document.getElementById(iconId);
            
            if (input.type === 'password') {
                input.type = 'text';
                icon.classList.remove('bi-eye');
                icon.classList.add('bi-eye-slash');
            } else {
                input.type = 'password';
                icon.classList.remove('bi-eye-slash');
                icon.classList.add('bi-eye');
            }
        }
    </script>
}
